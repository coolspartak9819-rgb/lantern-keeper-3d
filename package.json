{
  "name": "lantern-keeper-3d",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "npm run wasm:build && npm run assets:palette && vite",
    "wasm:build": "sh scripts/build-wasm.sh",
    "assets:palette": "sh scripts/build-palette.sh",
    "build": "npm run wasm:build && npm run assets:palette && vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "three": "^0.170.0"
  },
  "devDependencies": {
    "vite": "^6.0.0"
  }
}
